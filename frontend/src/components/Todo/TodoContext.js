import { createContext } from 'react'

export const TodoContext = createContext('empty')

export const TodoDetailsContext = createContext('empty')

export const TodoFormContext = createContext('empty')
import styled from 'styled-components'
import { v } from '../../../assets/styles/variables'

export const STodosContainer = styled.div`
	position: relative;
	flex: 1;
	display: flex;
	gap: ${v.mdSpacing};
	flex-direction: column;
`
